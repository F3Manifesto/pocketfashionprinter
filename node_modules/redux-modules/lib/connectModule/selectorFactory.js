'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _redux = require('redux');

var _toObject = require('../utils/toObject');

var _toObject2 = _interopRequireDefault(_toObject);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function mapDispatch(dispatch, modules) {
  function bindDispatch(_ref) {
    var actions = _ref.actions;

    return (0, _redux.bindActionCreators)(actions, dispatch);
  }

  if (modules.length === 1) {
    return {
      actions: bindDispatch(modules[0])
    };
  }

  return {
    actions: (0, _toObject2.default)(modules, 'name', bindDispatch)
  };
}

var selectorFactory = function selectorFactory(mapState, modules) {
  return function (dispatch, factoryOptions) {
    return function (state, ownProps) {
      var props = _extends({}, mapState(state, ownProps), mapDispatch(ownProps.dispatch || dispatch, modules), ownProps);

      return props;
    };
  };
};

exports.default = selectorFactory;